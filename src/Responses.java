
/**
 * List of responses for general and specific uses. Used to relay more specific
 * information back to the client
 */
public enum Responses {
	BAD_USERNAME_FORMAT, USERNAME_TAKEN, UNKNOWN_FAILURE, INTERNAL_SERVER_ERROR, NOT_FOUND, BAD_PASSWORD, SUCCESS,
	EXISTS, SILLY, NO_USER_NAME, FAILURE
}
